# VPS Management Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      GitHub Repository                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              VPS Management System                          │ │
│  │                                                              │ │
│  │  ┌──────────────┐     ┌────────────────┐                  │ │
│  │  │   Scripts    │     │  Workflows     │                  │ │
│  │  ├──────────────┤     ├────────────────┤                  │ │
│  │  │ init-vps.sh  │     │ vps-           │                  │ │
│  │  │ setup-vps.sh │────▶│ management.yml │                  │ │
│  │  └──────────────┘     └────────────────┘                  │ │
│  │                              │                              │ │
│  │  ┌──────────────────────────┼─────────────────────────┐   │ │
│  │  │    deploy-config.json    │                         │   │ │
│  │  │  ┌────────────────┐      │                         │   │ │
│  │  │  │ Servers        │      │                         │   │ │
│  │  │  │ - server-us    │      │                         │   │ │
│  │  │  │ - server-dublin│      │                         │   │ │
│  │  │  │ - server-sgp   │      │                         │   │ │
│  │  │  └────────────────┘      │                         │   │ │
│  │  └──────────────────────────┼─────────────────────────┘   │ │
│  └──────────────────────────────┼────────────────────────────┘ │
│                                 │                               │
│  ┌──────────────────────────────┼────────────────────────────┐ │
│  │         GitHub Secrets       │                            │ │
│  │  - SSH_PRIVATE_KEY ──────────┘                            │ │
│  │  - AWS_ACCESS_KEY_ID (optional)                           │ │
│  │  - AWS_SECRET_ACCESS_KEY (optional)                       │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │ SSH (via GitHub Actions)
                                │
            ┌───────────────────┼───────────────────┐
            │                   │                   │
            ▼                   ▼                   ▼
    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
    │  Server US   │    │Server Dublin │    │ Server SGP   │
    ├──────────────┤    ├──────────────┤    ├──────────────┤
    │ Ubuntu 22.04 │    │ Ubuntu 22.04 │    │ Ubuntu 22.04 │
    │              │    │              │    │              │
    │ ┌──────────┐ │    │ ┌──────────┐ │    │ ┌──────────┐ │
    │ │  Nginx   │ │    │ │  Nginx   │ │    │ │  Nginx   │ │
    │ │  Sites:  │ │    │ │  Sites:  │ │    │ │  Sites:  │ │
    │ │ tikmatrix│ │    │ │ tikmatrix│ │    │ │ tikmatrix│ │
    │ │ igmatrix │ │    │ │ igmatrix │ │    │ │ igmatrix │ │
    │ │ ytmatrix │ │    │ │ ytmatrix │ │    │ │ ytmatrix │ │
    │ └──────────┘ │    │ └──────────┘ │    │ └──────────┘ │
    │              │    │              │    │              │
    │ ┌──────────┐ │    │ ┌──────────┐ │    │ ┌──────────┐ │
    │ │ Certbot  │ │    │ │ Certbot  │ │    │ │ Certbot  │ │
    │ │ SSL Auto │ │    │ │ SSL Auto │ │    │ │ SSL Auto │ │
    │ └──────────┘ │    │ └──────────┘ │    │ └──────────┘ │
    │              │    │              │    │              │
    │ ┌──────────┐ │    │ ┌──────────┐ │    │ ┌──────────┐ │
    │ │Fail2Ban  │ │    │ │Fail2Ban  │ │    │ │Fail2Ban  │ │
    │ │   UFW    │ │    │ │   UFW    │ │    │ │   UFW    │ │
    │ └──────────┘ │    │ └──────────┘ │    │ └──────────┘ │
    │              │    │              │    │              │
    │ deploy user  │    │ deploy user  │    │ deploy user  │
    └──────────────┘    └──────────────┘    └──────────────┘
```

## Workflow Operations

```
┌────────────────────────────────────────────────────────────────┐
│              VPS Management Workflow                            │
│                 (Manual Trigger)                                │
└────────────────────────────────────────────────────────────────┘
                             │
                             ▼
                ┌────────────────────────┐
                │  Choose Operation      │
                └────────────────────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                  ▼                  ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │  deploy  │      │configure │      │  push    │
    │   site   │      │  nginx   │      │  files   │
    └──────────┘      └──────────┘      └──────────┘
          │                  │                  │
          │                  │                  │
          ▼                  ▼                  ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │  health  │      │  manage  │      │  renew   │
    │  check   │      │  backup  │      │   ssl    │
    └──────────┘      └──────────┘      └──────────┘
          │
          ▼
    ┌──────────┐
    │  update  │
    │   site   │
    └──────────┘
```

## Operation Flow Details

### 1. Deploy Site Operation

```
GitHub Actions
    │
    ▼
┌─────────────────┐
│ Checkout Code   │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Install Node.js │
│ & Dependencies  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Brand Replace   │
│ (if needed)     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Build Site      │
│ (npm run build) │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Create Archive  │
│ (.tar.gz)       │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Upload to VPS   │
│ (via SCP)       │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Backup Existing │
│ Deployment      │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Extract Archive │
│ to Web Root     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Set Permissions │
│ (deploy:www-data│
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ ✅ Deployment   │
│   Complete!     │
└─────────────────┘
```

### 2. Configure Nginx Operation

```
GitHub Actions
    │
    ▼
┌─────────────────┐
│ Connect to VPS  │
│ (via SSH)       │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Determine       │
│ Domain Type     │
│ (root/subdomain)│
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Generate Nginx  │
│ Configuration   │
│ (static/proxy)  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Create Web Root │
│ or Upstream     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Test Config     │
│ (nginx -t)      │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Reload Nginx    │
└─────────────────┘
    │
    ▼
┌─────────────────┐      ┌─────────────────┐
│ SSL Requested?  │─Yes─▶│ Run Certbot     │
└─────────────────┘      │ HTTP/Route53    │
    │                    └─────────────────┘
    │ No                          │
    ▼                             ▼
┌─────────────────┐      ┌─────────────────┐
│ ✅ Site Ready   │◀─────│ ✅ SSL Issued   │
│   (HTTP only)   │      │   (HTTPS)       │
└─────────────────┘      └─────────────────┘
```

### 3. Health Check Operation

```
GitHub Actions
    │
    ▼
┌─────────────────────────┐
│ Connect to All Servers  │
│ (Parallel)              │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Gather System Info      │
│ - OS, Uptime            │
│ - Disk, Memory          │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Check Services          │
│ - Nginx Status          │
│ - Firewall (UFW)        │
│ - Fail2Ban              │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ List Configurations     │
│ - Configured Sites      │
│ - SSL Certificates      │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ 📊 Display Report       │
│ (in GitHub Actions log) │
└─────────────────────────┘
```

## Setup Flow

### Initial VPS Setup (One-Time)

```
┌─────────────────┐
│ New VPS         │
│ (Ubuntu 22.04)  │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ SSH as root     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Download        │
│ init-vps.sh     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Set Environment │
│ Variables:      │
│ - GITHUB_PUBKEY │
│ - SSH_PORT      │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Run init-vps.sh │
│ (as root)       │
└─────────────────┘
    │
    ▼
┌─────────────────────────────┐
│ Automated Installation:     │
│ ✅ System updates           │
│ ✅ Deploy user + SSH        │
│ ✅ Nginx, Certbot           │
│ ✅ Firewall (UFW)           │
│ ✅ Fail2Ban                 │
│ ✅ Security hardening       │
└─────────────────────────────┘
    │
    ▼
┌─────────────────┐
│ ✅ VPS Ready    │
│ for GitHub      │
│ Actions         │
└─────────────────┘
```

### GitHub Configuration

```
┌─────────────────┐
│ GitHub Repo     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Add Secrets:    │
│ SSH_PRIVATE_KEY │
│ AWS_* (optional)│
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Update          │
│ deploy-config   │
│ .json           │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ ✅ Ready to     │
│ Deploy!         │
└─────────────────┘
```

## Security Architecture

```
┌──────────────────────────────────────────────────────────┐
│                    Security Layers                        │
└──────────────────────────────────────────────────────────┘

Layer 1: Network Security
┌────────────────────────────────────────────────────────┐
│ UFW Firewall                                            │
│ ├─ SSH Port (22 or custom) ✅                          │
│ ├─ HTTP (80) ✅                                         │
│ ├─ HTTPS (443) ✅                                       │
│ └─ All other ports ❌ DENIED                           │
└────────────────────────────────────────────────────────┘

Layer 2: Intrusion Prevention
┌────────────────────────────────────────────────────────┐
│ Fail2Ban                                               │
│ ├─ SSH Brute Force Protection (3 attempts)            │
│ ├─ Nginx HTTP Auth Protection                         │
│ └─ Rate Limit Violations                              │
└────────────────────────────────────────────────────────┘

Layer 3: Authentication
┌────────────────────────────────────────────────────────┐
│ SSH Key-Based Authentication                           │
│ ├─ No password authentication                         │
│ ├─ ED25519 keys (stronger than RSA)                   │
│ └─ Deploy user (non-root)                             │
└────────────────────────────────────────────────────────┘

Layer 4: Authorization
┌────────────────────────────────────────────────────────┐
│ Sudo Permissions (Minimal)                             │
│ ├─ systemctl reload/restart nginx                     │
│ ├─ chown/chmod /var/www                               │
│ ├─ certbot operations                                 │
│ └─ No other root privileges                           │
└────────────────────────────────────────────────────────┘

Layer 5: File Permissions
┌────────────────────────────────────────────────────────┐
│ Web Files                                              │
│ ├─ Owner: deploy                                       │
│ ├─ Group: www-data                                     │
│ ├─ Permissions: 775 (rwxrwxr-x)                       │
│ └─ Nginx runs as www-data (read-only)                │
└────────────────────────────────────────────────────────┘

Layer 6: Application Security
┌────────────────────────────────────────────────────────┐
│ Nginx Configuration                                    │
│ ├─ Hide version (server_tokens off)                   │
│ ├─ Security headers (XSS, CSP, etc.)                  │
│ ├─ Rate limiting                                       │
│ ├─ Deny access to hidden files                        │
│ └─ Block common attack patterns                       │
└────────────────────────────────────────────────────────┘

Layer 7: Encryption
┌────────────────────────────────────────────────────────┐
│ SSL/TLS (Let's Encrypt)                                │
│ ├─ Automatic certificate issuance                     │
│ ├─ Auto-renewal (daily check)                         │
│ ├─ HTTP → HTTPS redirect                              │
│ └─ Strong cipher suites                               │
└────────────────────────────────────────────────────────┘
```

## File Organization

```
www.tikmatrix.com/
├── .github/
│   └── workflows/
│       ├── deploy.yml              (existing deployment)
│       └── vps-management.yml      (new: VPS operations)
│
├── scripts/
│   ├── init-vps.sh                 (new: non-interactive setup)
│   ├── setup-vps.sh                (existing: interactive setup)
│   ├── fix-deploy-permissions.sh   (existing: permissions fix)
│   └── README.md                   (new: scripts guide)
│
├── docs/
│   ├── vps-management.md           (new: full docs, Chinese)
│   ├── vps-management.en.md        (new: full docs, English)
│   ├── vps-management-quickstart.md (new: quick start)
│   └── workflow-examples.md        (new: 10 examples)
│
└── deploy-config.json              (existing: server config)
```

## Technology Stack

```
┌─────────────────────────────────────────────────────────────┐
│                    Technology Stack                          │
└─────────────────────────────────────────────────────────────┘

CI/CD:
├─ GitHub Actions (workflow automation)
├─ GitHub Secrets (credential storage)
└─ SSH (secure remote execution)

Server OS:
├─ Ubuntu 22.04 LTS (primary)
└─ Debian 11+ (compatible)

Web Server:
├─ Nginx (latest stable)
├─ Static site serving
└─ Reverse proxy support

SSL/TLS:
├─ Let's Encrypt (free certificates)
├─ Certbot (automatic management)
├─ HTTP-01 validation
└─ DNS-01 validation (Route53)

Security:
├─ UFW (Uncomplicated Firewall)
├─ Fail2Ban (intrusion prevention)
├─ SSH key authentication
└─ Security headers

Deployment:
├─ SCP (file transfer)
├─ Rsync (efficient sync)
├─ Tar/Gzip (compression)
└─ Automated backups

Monitoring:
├─ Health check scripts
├─ Nginx logs
├─ System metrics
└─ GitHub Actions logs

Build Tools:
├─ Node.js 20+
├─ npm
├─ Docusaurus 3.7+
└─ Custom build scripts
```

## Backup Strategy

```
Automatic Backups:
┌─────────────────────────────────────────────────────────────┐
│ Before Each Deployment                                       │
│ ├─ Create tar.gz of current site                           │
│ ├─ Store in /var/backups/www/                              │
│ ├─ Name: {domain}-{timestamp}.tar.gz                       │
│ └─ Keep last 3 backups, delete older                       │
└─────────────────────────────────────────────────────────────┘

Manual Backups:
┌─────────────────────────────────────────────────────────────┐
│ Via GitHub Actions                                          │
│ ├─ operation: manage-backup                                │
│ ├─ backup_action: create                                   │
│ ├─ backup_site: domain.com                                 │
│ └─ Name: {domain}-manual-{timestamp}.tar.gz                │
└─────────────────────────────────────────────────────────────┘

Restoration:
┌─────────────────────────────────────────────────────────────┐
│ Manual Process (SSH required)                               │
│ 1. SSH into server                                          │
│ 2. cd /var/backups/www                                      │
│ 3. tar -xzf backup.tar.gz -C /var/www.domain.com           │
│ 4. sudo chown -R deploy:www-data /var/www.domain.com       │
│ 5. sudo systemctl reload nginx                             │
└─────────────────────────────────────────────────────────────┘
```

## Scalability

```
Current: 3 servers (US, Dublin, Singapore)
Future: Add more servers easily

┌─────────────────────────────────────────────────────────────┐
│ To Add New Server:                                          │
│                                                              │
│ 1. Run init-vps.sh on new server                           │
│ 2. Add to deploy-config.json                               │
│ 3. Commit and push                                          │
│ 4. GitHub Actions automatically includes new server        │
│                                                              │
│ ✅ Parallel deployment to all servers                      │
│ ✅ No workflow changes needed                              │
│ ✅ Consistent configuration                                │
└─────────────────────────────────────────────────────────────┘
```

## Monitoring & Logs

```
GitHub Actions Logs:
├─ Workflow execution history
├─ Per-server deployment status
├─ Error messages and stack traces
└─ Health check reports

Server Logs:
├─ /var/log/tikmatrix-init.log (initialization)
├─ /var/log/nginx/{domain}.access.log (access logs)
├─ /var/log/nginx/{domain}.error.log (error logs)
└─ /var/log/nginx/error.log (Nginx global errors)

System Logs:
├─ /var/log/auth.log (SSH, authentication)
├─ /var/log/syslog (system messages)
└─ fail2ban-client status (ban status)
```

## Performance Optimization

```
Nginx Optimizations:
├─ Worker processes = CPU cores
├─ Worker file limit = 65535
├─ Keepalive connections
├─ Gzip compression
├─ Static asset caching (365 days)
└─ Rate limiting (10 req/s + burst 20)

System Optimizations:
├─ File descriptor limits increased
├─ Kernel parameters tuned
├─ Shared memory hardened
└─ SYN cookie protection enabled

Deployment Optimizations:
├─ Parallel server deployments
├─ Rsync for incremental updates
├─ Compressed archives
└─ Efficient tar extraction
```
